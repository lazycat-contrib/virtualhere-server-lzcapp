# manifest: 指定 lpk 包的 manifest.yml 文件路径
manifest: ./manifest.yml
# pkgout: lpk 包的输出路径
pkgout: ./
contentdir: ./dist
# icon 仅仅允许 png 后缀的文件
icon: ./icon.png

compose_override:
  services:
    virtualhere-server:
      privileged: true
